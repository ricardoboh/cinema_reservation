spring:
  application:
    name: cinema-reservation-test
  datasource:
    url: jdbc:h2:mem:cinema-test;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  data:
    jdbc:
      repositories:
        enabled: false

